import React from 'react';
import '../style/title.less';

const Tittle = ({ title }) => {
  return (
    <div className="category">
      <h2>&nbsp;{title}</h2>
    </div>
  );
};

export default Tittle;
